# Lilith Brochure Review Site

This folder is ready to publish with GitHub Pages using the `docs/` source.

Recommended Pages setting:

- Source: Deploy from a branch
- Branch: `main`
- Folder: `/docs`

`index.html` is the live review page.
`versions/` stores archived snapshots.
